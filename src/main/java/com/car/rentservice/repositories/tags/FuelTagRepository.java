package com.car.rentservice.repositories.tags;

import com.car.rentservice.modal.tags.FuelTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FuelTagRepository extends JpaRepository<FuelTag, Long> {
}
