package com.car.rentservice.repositories.tags;

import com.car.rentservice.modal.tags.WdTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WDTagRepository extends JpaRepository<WdTag, Long> {
}
