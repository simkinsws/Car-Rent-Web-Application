package com.car.rentservice.repositories.tags;

import com.car.rentservice.modal.tags.FuelConsumptionTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FuelConsumptionTagRepository extends JpaRepository<FuelConsumptionTag, Long> {
}
